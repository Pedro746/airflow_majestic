create table if not exists filial (
    id_filial int not null,
    filial varchar(20),
    fantasia varchar(20),
    cidade varchar(20),
    nomeEstado varchar(20),
    estado varchar(2),
    regiao varchar(20)

);